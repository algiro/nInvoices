# =================================
# nInvoices Docker Configuration
# =================================
# Copy this file to .env and update with your values
# NEVER commit .env to version control!
#
# ⚠️ IMPORTANT: How This Works
# These values configure NEW services that Docker will create.
# You're CHOOSING the passwords that will be SET during first startup.
# This is NOT for connecting to existing services!
#
# Example:
# If you set POSTGRES_PASSWORD=MyPassword123
# Docker will CREATE PostgreSQL with that password
# Then you can connect using: MyPassword123

# Environment (development or production)
ENVIRONMENT=development

# =================================
# PostgreSQL Configuration
# =================================
# These values will be used to CREATE the PostgreSQL user and database
# on first startup. Choose secure passwords here.

POSTGRES_USER=ninvoices_user
POSTGRES_PASSWORD=change_me_secure_password_123
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Application Database
POSTGRES_DB=ninvoices_db

# Keycloak Database (separate)
KEYCLOAK_DB_NAME=keycloak_db

# =================================
# Keycloak Configuration
# =================================
# Admin credentials for Keycloak management console
# These will be set on first startup

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_me_admin_password_456

# Keycloak URLs (change for production)
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=ninvoices
KEYCLOAK_CLIENT_ID=ninvoices-api
KEYCLOAK_CLIENT_SECRET=change_me_client_secret_789

# =================================
# Application Configuration
# =================================
# API Base URL
API_URL=http://localhost:5000

# Frontend URL
WEB_URL=http://localhost:3000

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# =================================
# Ports Configuration
# =================================
# Exposed ports on host machine
API_PORT=5000
WEB_PORT=3000
KEYCLOAK_PORT=8080
POSTGRES_PORT=5432

# =================================
# Volume Paths (relative to docker directory)
# =================================
POSTGRES_DATA_PATH=./volumes/postgres
KEYCLOAK_DATA_PATH=./volumes/keycloak
APP_LOGS_PATH=./volumes/logs

# =================================
# Production Only Settings
# =================================
# Set these for production deployment

# Your domain
DOMAIN=your-domain.com

# SSL/TLS Configuration
SSL_CERT_PATH=./volumes/ssl/cert.pem
SSL_KEY_PATH=./volumes/ssl/key.pem

# Production URLs (use HTTPS)
# KEYCLOAK_URL=https://auth.your-domain.com
# API_URL=https://api.your-domain.com
# WEB_URL=https://your-domain.com
